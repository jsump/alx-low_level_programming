#ifndef main_h
#define main_h
#include<stdio.h>

void postitive_or_negative(int i);
int largest_number(int a, int b, int c);
void print_remaining_days(int month, int day, int year);
inr convert_day(int month, int day);
#endif

